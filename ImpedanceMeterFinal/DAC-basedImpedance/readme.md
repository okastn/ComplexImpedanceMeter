DAC-Based Version

Uses internal DAC and a lookup table (LUT) to generate a cosine waveform

Timer interrupt updates DAC output per sample

Dual ADC sampling synchronized via TIM6

Ideal for simpler hardware setups and wider impedance range measurements
